package assignment2.model;

public enum UpdateProductState {

    SUCCESS,
    QUANTITY_ERROR,
    CATEGORY_ERROR,
    CODE_RANGE_ERROR,
    CODE_DUPLICATE_ERROR,
    DUPLICATE_PRODUCT_ERROR,
    PRICE_ERROR
}
